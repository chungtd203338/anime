<style>
    body {
        background-color: #FFE5E7;
    }
</style>
<div class="mt-4">
    <div class="row">
        {{#each films}}
        <div class="col-sm-6 col-lg-4">
            <div class="card card-film-item">
                <a href="/films/{{this.slug}}">
                    <img class="card-img-top" src="{{this.image}}" alt="{{this.name}}" />
                </a>
                <div class="card-body">
                    <a href="/films/{{this.slug}}">
                        <h5 class="card-title">{{this.name}}</h5>
                    </a>
                    <p class="card-text">{{this.description}}</p>
                </div>
            </div>
        </div>
        {{/each}}

        <div class="col-lg-0.1">
            <div class="abc"></div>
        </div>
    </div>
</div>

<script>
    var films = document.getElementsByTagName("a");
    for (filmIndex in films) {
        console.log(films[filmIndex].href);
    }
</script>